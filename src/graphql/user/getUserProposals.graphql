query getUserProposals {
  me {
    proposals {
      primaryKey
      shortCode
      title
      status {
        ...proposalStatus
      }
      publicStatus
      statusId
      created
      finalStatus
      notified
      submitted
      proposer {
        id
      }
      call {
        id
        shortCode
        isActive
      }
    }
  }
}
