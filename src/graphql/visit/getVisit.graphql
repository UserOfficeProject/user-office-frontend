query getVisit($visitId: Int!) {
  visit(visitId: $visitId) {
    ...visit
    userVisits {
      ...userVisit
      user {
        ...basicUserDetails
      }
    }
    proposal {
      ...proposal
      instrument {
        name
      }
    }
  }
}
