query getVisit($visitId: Int!) {
  visit(visitId: $visitId) {
    ...visit
    questionary {
      ...questionary
    }
    userVisits {
      ...userVisit
      user {
        ...basicUserDetails
      }
    }
    proposal {
      ...proposal
      instrument {
        name
      }
    }
  }
}
