mutation removeMemberFromSEPProposal(
  $memberId: Int!
  $sepId: Int!
  $proposalPK: Int!
) {
  removeMemberFromSEPProposal(
    memberId: $memberId
    sepId: $sepId
    proposalPK: $proposalPK
  ) {
    rejection {
      ...rejection
    }
    sep {
      id
    }
  }
}
