mutation assignSepReviewersToProposal(
  $memberIds: [Int!]!
  $sepId: Int!
  $proposalPK: Int!
) {
  assignSepReviewersToProposal(
    memberIds: $memberIds
    sepId: $sepId
    proposalPK: $proposalPK
  ) {
    rejection {
      ...rejection
    }
    sep {
      id
    }
  }
}
