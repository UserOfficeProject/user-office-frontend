query getSEPReviewers($sepId: Int!) {
  sepReviewers(sepId: $sepId) {
    userId
    sepId
    roles {
      id
      shortCode
      title
    }
    user {
      id
      firstname
      lastname
      organisation
      position
      placeholder
      created
    }
  }
}
